apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: lava-data
  name: lava-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
